/** Legacy cap for progress when a job has no stored plan (should not occur in production). */
export const INTERVIEW_MAX_TURNS = 10;

/** Every job must have exactly this many pre-generated screening questions. */
export const PLANNED_INTERVIEW_QUESTION_COUNT = 10;
